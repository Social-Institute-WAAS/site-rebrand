# Update Waas' site layout 
Update Waas' site layout, based on new graphic project updating.


Node >= 10.0.0

### Installar pacotes
`npm install` ou 
`yarn install`

### Rodar servidor
`npm run start` ou
`yarn start`

### Gerar o build
`npm run build` ou
`yarn build`
